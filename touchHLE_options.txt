# This file lets you specify the options you want to use for various apps.
#
# Options in this file take precedence over ones in touchHLE_default_options.txt
#
# ---
#
# Each line in this file specifies options for an app; there should only be one
# line per app. A line should consist of the identifier for an app, followed by
# a colon (:), followed by the options to apply. To find out an app's
# identifier, you can run it in touchHLE and look at the “App bundle info”
# output. To get a list of possible options, see OPTIONS_HELP.txt or run
# touchHLE with the --help flag.
#
# Text on a line following a hash sign (#) is treated as a comment and ignored.
# Empty lines are also ignored.

# Puzzle Agent renders for a landscape iPhone but does not declare an
# interface orientation in Info.plist. Start it in the matching orientation.
com.telltalegames.Grickle101Low: --landscape-left

# Puzzle Agent HD renders in landscape on iPad, but its Info.plist causes the
# legacy orientation fallback to select PortraitUpsideDown.
com.telltalegames.Grickle101: --landscape-left

# Early Telltale iPad/iPhone engine builds use the GLES2 subset provided by
# touchHLE's desktop OpenGL 2.1 compatibility backend.
com.telltalegames.SamMax301: --landscape-left --gles2-compat
com.telltalegames.WallaceGromit102: --landscape-left --gles2-compat
